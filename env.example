# Frontend
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# WhatsApp (for webhook + sending messages)
# Global token used for webhook verification (Meta GET challenge)
WHATSAPP_VERIFY_TOKEN=mafal_verify_token_2024
# App secret for signature validation (x-hub-signature-256)
WHATSAPP_APP_SECRET=
# Access token for Graph API (used as fallback if restaurant-specific token is not set)
WHATSAPP_ACCESS_TOKEN=
# Optional: concierge phone number id to route multi-restaurant assistant
WHATSAPP_CONCIERGE_PHONE_NUMBER_ID=

# AI (Genkit / Gemini)
GOOGLE_GENKIT_API_KEY=

# Database (Prisma)
DATABASE_URL=

# Enable demo mode to bypass DB/AI and simply echo inbound messages
# Values: true|false or 1|0
DEMO_MODE=true

# Backend (if needed in future)
# OPENAI_API_KEY=
