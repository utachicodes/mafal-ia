# Frontend
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# AI (Genkit / Gemini)
GOOGLE_GENKIT_API_KEY=

# Database (Prisma)
# Pooled connection (e.g., Neon "-pooler" URL). Required in production.
DATABASE_URL=
# Direct connection (optional). If unsure, set same as DATABASE_URL.
DIRECT_URL=

# Backend (if needed in future)
# OPENAI_API_KEY=
